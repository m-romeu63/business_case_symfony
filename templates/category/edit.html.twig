{% extends 'base.html.twig' %}

{% block title %}Édition de catégorie{% endblock %}

{% block body %}
<header>
    {{ include('_header.html.twig') }}
</header>
    <h2 class="text-center mt-2">Édition de catégorie</h2>
    <div class="col-lg-9 mx-auto my-4">
        {{ include('category/_form.html.twig', {'button_label': 'Éditer'}) }}

        <a href="{{ path('category_index') }}">Retour à la liste</a>

        {{ include('category/_delete_form.html.twig') }}
    </div>
    <footer>
        {{ include('includes/footer.php') }}
    </footer>
{% endblock %}
